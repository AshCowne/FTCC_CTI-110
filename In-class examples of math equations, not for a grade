# Ash Cowne
# 10/3/2024
# In-class examples of math equations, not for a grade.

#Import random libary for use in the program.
import random

'''
# Get the value of the sides from the user.
side_1 = float(input("Input for one side: "))
side_2 = float(input("Input for another side: "))
'''

# Generate random values for side one and side two.
side_1 = random.randint(1,1000);
side_2 = random.randint(1,1000);

#Caculate the hypontenuse from the sides.
hypontenuse = ((side_1 ** 2) + (side_2 ** 2))**0.5

#Printing out the answer.
print(f"The hypontenuse for this right triangle with the sides of {side_1} and {side_2} two sides is {hypontenuse}.");
